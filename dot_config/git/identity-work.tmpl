# Pulled in by ~/.gitconfig via includeIf; see dot_gitconfig.tmpl.
# No sshCommand here: the work key is the machine-wide default, set in the
# global [core] section of ~/.gitconfig.
[user]
	email = "{{ .workEmail }}"
	signingkey = {{ .workSigningKey }}
