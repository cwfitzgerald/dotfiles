# Pulled in by ~/.gitconfig via includeIf; see dot_gitconfig.tmpl.
[user]
	email = "{{ .workEmail }}"
	signingkey = {{ .workSigningKey }}
[core]
	# See identity-open for how key routing works.
	sshCommand = ssh -i ~/.ssh/github_work_auth.pub
