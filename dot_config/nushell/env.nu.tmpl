# env.nu
#
# Installed by:
# version = "0.103.0"
#
# Previously, environment variables were typically configured in `env.nu`.
# In general, most configuration can and should be performed in `config.nu`
# or one of the autoload directories.
#
# This file is generated for backwards compatibility for now.
# It is loaded before config.nu and login.nu
#
# See https://www.nushell.sh/book/configuration.html
#
# Also see `help config env` for more options.
#
# You can remove these comments if you want or leave
# them for future reference.

# mise's nu activation is generated code that hardcodes the mise binary path, so
# regenerate it every startup rather than checking it in. config.nu `use`s the
# module unconditionally, so write an empty one when mise isn't installed.
let mise_module = $nu.default-config-dir | path join "mise.nu"
if (which mise | is-not-empty) {
    ^mise activate nu | save --force $mise_module
} else {
    "" | save --force $mise_module
}
