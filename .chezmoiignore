{{/* Repo-only docs; they describe the source tree and must not land in ~. */}}
README.md
AGENTS.md
CLAUDE.md
{{- if ne .chezmoi.os "windows" }}
AppData/**
Documents/**
.claude/hooks/*.ps1
.codex/hooks/*.ps1
{{- end }}
{{- if ne .chezmoi.os "darwin" }}
Library/**
.ssh/config
.claude/hooks/*.sh
.codex/hooks/*.sh
{{- end }}
{{- if not (get . "workEmail") }}
{{/* Single-identity machine: the per-directory identity split is not deployed. */}}
.config/jj/conf.d
.config/jj/conf.d/**
.config/git
.config/git/identity-*
{{- end }}
{{- if not (get . "workEmail") }}
{{/* Single identity, so ~/.ssh/config has no per-directory Match blocks and
     nothing would point IdentitiesOnly at these. */}}
.ssh/github_open_auth.pub
.ssh/github_work_auth.pub
{{- end }}
